# include<stdio.h>
void main(){
    int a,b;
    printf("Enter two numbers for a & b: ");
    scanf("%d %d",&a,&b);
    if(a>b){
        printf("Largest among the two is: %d",a);
    }else{
        printf("Largest among the two is: %d",b);
    }
}
